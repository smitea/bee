{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the Bee Client main window. Principle: \"does not need it\" wins over \"convenient to have\". All file I/O (SQLite, plugin registry, logs) is performed by the Rust application layer through internal APIs; only capabilities invoked from the WebView need to be granted. No shell, no fs, no arbitrary http, no dialogs, no clipboard, no notification beyond core defaults.",
  "windows": [
    "main"
  ],
  "permissions": [
    "core:default"
  ]
}

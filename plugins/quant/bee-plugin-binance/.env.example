# Sample Datasource config for the Binance plugin (S34).
# This is a TEMPLATE — copy to a real config file (e.g., binance.example.json)
# and supply real credentials. The real config file should NOT be checked
# into version control (add to .gitignore).
#
# Usage:
#   cp .env.example binance.example.json
#   # edit binance.example.json with real values
#   bee datasource create binance --adapter binance_subscribe --config @binance.example.json

{
  "ws_url":             "wss://stream.binance.com:9443",
  "rest_url":           "https://api.binance.com",
  "api_key":            "",
  "api_secret":         "",
  "rate_limit_per_sec": 10,
  "tenant":             0
}

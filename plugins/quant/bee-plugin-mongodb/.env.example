# Sample Datasource config for the MongoDB plugin (S37).
# This is a TEMPLATE — copy to a real config file (e.g., mongodb.example.json)
# and supply real credentials. The real config file should NOT be checked
# into version control (add to .gitignore).
#
# Usage:
#   cp .env.example mongodb.example.json
#   # edit mongodb.example.json with real values
#   bee datasource create mongodb --adapter mongodb_insert --config @mongodb.example.json

{
  "uri":       "mongodb://localhost:27017",
  "database":  "trading",
  "username":  "",
  "password":  "",
  "app_name":  "bee",
  "tls":       false,
  "tenant":    0
}

# Sample Datasource config for the InfluxDB v2 plugin (S36).
# This is a TEMPLATE — copy to a real config file (e.g., influxdb.example.json)
# and supply real credentials. The real config file should NOT be checked
# into version control (add to .gitignore).
#
# Usage:
#   cp .env.example influxdb.example.json
#   # edit influxdb.example.json with real values
#   bee datasource create influxdb --adapter influxdb_write --config @influxdb.example.json

{
  "url":        "http://localhost:8086",
  "token":      "",
  "org":        "",
  "bucket":     "quant",
  "timeout_ms": 5000,
  "tenant":     0
}

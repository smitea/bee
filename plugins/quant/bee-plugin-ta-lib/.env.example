# Plugin-level config for the ta-indicators plugin (S38).
# This is a TEMPLATE — copy to a real config file (e.g., ta-indicators.example.json)
# and supply the actual config. The real config file should NOT be checked
# into version control (add to .gitignore).
#
# Note: this is a PLUGIN-level config (not a Datasource config). It's
# loaded once at plugin init, not per-Datasource.
#
# Usage:
#   cp .env.example ta-indicators.example.json
#   # edit if you need a non-default config
#   bee plugin load target/release/libbee_plugin_ta_indicators.dylib --config @ta-indicators.example.json

{
  "indicator_backend": "yata"
}

[package]
name = "bee-runtime"
version.workspace = true
edition.workspace = true
license.workspace = true
publish.workspace = true
description = "Bee 应用层: Pipeline / Phase / Handler / Dag / Compiler (数据面执行单元)"

[dependencies]
tokio = { workspace = true }
arrow-schema = "55"
bee-adapter = { workspace = true }
bee-types = { workspace = true }

[dev-dependencies]
tokio = { workspace = true }
bee-control = { workspace = true }

[lints]
workspace = true
